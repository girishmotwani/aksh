apiVersion: v1
kind: Namespace
metadata:
  name: aksh-system
  labels:
    app.kubernetes.io/part-of: aksh
    # The injector namespace itself is not an injection target.
    aksh.dev/inject: disabled
